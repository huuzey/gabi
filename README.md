steps to run the code
first of if you don't have node js installed you need to have it installed by searching for node js and download for your operating system.
then unzip this file and run

1."npm install" for windows or "yarn add" for mac.
2."npm run dev" for windows or "yarn dev" for mac.
